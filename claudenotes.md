2026-06-03 11:40 — 1a:inspectSTT — AUDITED all SPT untaint logic (read-only). Live config: fwdUntaint=1 bwdUntaint=1 untaintTier=0 idealUntaint=0. Four mechanisms: (1) VP/ReachedVP = the root, resolved-not-committed = the one known race; (2) FORWARD untaint (rob:799, sound — non-load w/ all-safe args; !isMemTransmit=!isLoad&&!isStore guards a load's dest); (3) BACKWARD untaint (rob:850, SPT/Rutvik — ADD/SUB algebraic src=dest∓other + MOV; info-theoretically sound, inherits VP-race, most intricate→spot-check); (4) TIER uncond untaint of RBP/RSP/RSI/RDI = genuinely UNSAFE but OFF (tier=0) and MUST stay off (gadgets address thru those regs). Apply = setPartialTaint byte-gran (rob:988) — x86 subreg is the one impl spot to watch. VERDICT: conditions sound; fwd/bwd SPREAD the VP-race untaint but don't WORSEN it; only too-early untaint = VP gate. Differential available (untaint-ON SPT_6_oneLP/_full vs OFF SPT_6_disableUntaint, same-testset only) to confirm. See claudelog 11:40.

2026-06-03 11:15 — 1a:inspectSTT — RULED OUT a "taint-not-yet-set" window in STT (the stage-of-tainting question). STT taints at COMMIT (rob->compute_taint @commit_impl.hh:1612 via markCompletedInsts@728 — UNCONDITIONAL each tick, NOT starved by the fnc-stall which only blocks commitInsts progress; rename does 0 taint). No transmit-before-taint hole: fence=isArgsTainted recomputed every IEW tick BEFORE executeInsts (lsq:1013, iew:1548<1551); compute_taint taints loads EAGERLY on the static isAccess bit at dispatch (rob:810-812), pre-execution, single-pass head→tail; a tainted-addr load can't issue until its producer executed (cycles after dispatch) by which point the chain is tainted. ⚪ Commit-stage tainting is INEFFICIENT (O(ROB)/cyc, re-derives rename getArgProducer) but NOT a correctness bug. Only real STT/SPT hole stays the VP↔squash race (untaint on resolved/completed not committed; isPrevBrsCommitted exists, used only in a debug printf). Read-only. See claudelog 11:15.

2026-06-03 — 5:invisispec — RE-CORRECTION (supersedes my "const-addr FP" verdict): SpecLFB UV6's 68 hits are NOT const-addr FPs. Checked gadget asm 6/6: xmit = indexed load `movq (%base,%index),%dst`, addr=base+%index, %index from an earlier SPECULATIVE load (real load→addr Spectre gadget; dataflow intact). Runtime addr=0x3580 only b/c index VALUE=0 (zero-init buffer), NOT a constant operand. Under the dataflow signal the 68 ARE valid UV6 (tainted address load accesses cache in-window, SpecLFB isUnsafe=0 lets it). Zero index only blocks concrete leak demo, not the signal. ⚠️ PAPER_RESULTS.md baked my wrong const-addr verdict → @results owner please update. Fixed: claudelog, SPECLFB_UV6_REVIEW.md, project_speclfb memory.

2026-06-03 09:58 — 3a:runManagerNew — RESUMED recon-modded gem5 watchers (paused since 05:12) at jobs=12 (throttled from 40 to avoid the ~140-load saturation; box had 4 other-session watchers + 2 testsetgens). testrun --resume → campaigns results/STT_6__latest (79,901 pending) + STT_6_interleave__latest (386,500 pending), /work/gem5-recon-modded scheme2. NOTE: sweeping the OLD testsets/STT_6 (stale pre-RS), NOT the new testsets_latest 200k. Override cfgs runconfigs/run_config_*_j12.jsonc. pids 2796860/2796862. Configs were moved to runconfigs/ by another session. Testset gen (Recon/SPT taken) still finishing.

2026-06-03 — results — Compiled the RESULTS WRITEUP for Anna: **PAPER_RESULTS.md** (findings by defense: recon OTB+STL, STT VP-squash race & why we don't find the store/TLB bug, SPT same race; AMuLeT scope + the one in-scope load bug SpecLFB UV6; test-targeting 17%→100%; stall + fragility timing sweeps + why timing mods are legit; §8 = the still-missing AMuLeT timing/detection-efficiency run). **paper_figures/** = 17 candidate figs (3 per data slot, make_paper_figures.py reproducible, numbers baked from disk). **3 methods docs**: METHODS_LEAKAGE_KNOBS / _CONCRETIZATION / _INSTRUMENTATION. **UPDATE (per the 5:invisispec RE-CORRECTION above + owner): UV6 reframed to DEMONSTRATED** across PAPER_RESULTS §0/§3/§4/§9 + figs F2 + methods docs — const-addr is a *data-taint* disqualifier ONLY (doesn't apply to STT/SpecLFB; answers "const-addr shouldn't apply to STT?"). **AMuLeT A+B comparison DONE (measured)**: wired `cli.py fuzz --SpecLFB` to SpecLFB_AE (results/STT_6_speclfb/amulet_compare/). (A) SimSpect first UV6 @#6/23% vs AMuLeT @#39/3.3% = 6.5× targeting. (B) Anna: count JUST gem5 (no build/pipeview) → SimSpect hostSeconds 0.092 vs AMuLeT m5.simulate 0.057 s/test = COMPARABLE; the 40× was pipeview-measurement+fresh-boot (instrumentation). **Added: F8 efficiency figs (a/b/c), F9 big bug-table-by-defense, + paper_figures/figures.tex (TikZ/pgfplots/booktabs source; no TeX here so unverified-compile). 21 PNGs.** No SimSpect code/model/run touched. See claudelog.

2026-06-03 — 5:invisispec — SpecLFB UV6: mechanism CONFIRMED but 68 "leaks" = CONST-ADDR FPs (supersedes "72 candidates"). Gated checker on Speclfb isUnsafe → 68 unprotected-USL + 4 protected_lfb_access (control: SpecLFB DOES protect). Cache check: all read fixed paddr 0x3580, prologue-prewarmed → ReadReq HIT, no install, no leak. UV6 mechanism real (isCUSL=1,isUnsafe=0) but STT_6 loads aren't secret-addressed/cache-missing → 0 demonstrable leaks. Need AMuLeT-style secret-addr load gadget. Build+checker ready. Writeup rewritten SPECLFB_UV6_REVIEW.md. See claudelog.

2026-06-03 — 6:invisispec — SpecLFB INSTRUMENTED + UV6 mechanism CONFIRMED. Glue (no rebuild): scheme "2"→Speclfb, --branch-ann-base hex fix (was the all-tests-error blocker), parse_lsq memaccess_tick, check_ld_speclfb.py, results/STT_6_speclfb/. UV6: xmit loads isCUSL=1 but isReallyUnsafe=0 (first-spec-load exemption, AMuLeT Fig.8) → unprotected → 72/300 access cache in-window = leak CANDIDATES. OPEN: memaccess may over-approx L1 install (inst-014986 protected yet flagged) → next key on cache.cc install signal. Writeup results/STT_6_speclfb/SPECLFB_UV6_REVIEW.md. CleanupSpec audited: hooks ported but OUT of scope (intentionally installs+cleans up; bug=post-squash residue=final-cache-state). See claudelog.

2026-06-03 05:00 — 1a:inspectSTT — FINAL br_x (controlled matrix, supersedes my 04:30/03:30): the 46 "leaks" are CAUSED BY the fnc-commit-stall hook, NOT STT. fnc-stall=150→46/46 in-gap leak; fnc-stall=0→0/46 (gap ~620cyc→~5cyc). scheme STT==UNSAFE byte-identical everywhere → STT not involved, not delaying squash. ⚪ instrumentation/timing artifact (hook pins fnc at commit head → ROB can't drain → wrong-path re-speculates → xmit re-redirects in held window). 0 real STT branch leaks. ⚠️ OPEN: the LOAD race (19,845, the filed real bug) uses the same fnc-stall=150 — MUST re-check loads at fnc0 + STT-vs-Unsafe diff before trusting it's STT. Review md FINAL. See claudelog 05:00.
2026-06-03 04:30 — 1a:inspectSTT — (SUPERSEDED by 05:00) claimed 46 br_x = real STT VP-squash race at stock; WRONG cause — the "stock" run still had fnc-stall=150. See 05:00.

2026-06-03 — 6:invisispec — InvisiSpec×STT_6 ld run DONE: 0 leaks both modes (USLs squashed before visibility = defense holds; not_taken meaningful w/ 345 in-window spec reads, taken mostly load_never_executed). 43/3000 errors = gem5 cache.cc:192 hasRespData assert (per-test abort, config confirmed correct). Results: results/STT_6_invisispec/RESULTS.md. Does NOT cover AMuLeT eviction bug. SpecLFB assessed: hooks already ported, ~15min scheme glue + ~1-2h parse_lsq trace-format extension (newer gem5 base) to test UV6; awaiting go-ahead. See claudelog.

2026-06-03 — 6:invisispec — Wrote docs/AMULET_VS_SIMSPECT_SCOPE.md: AMuLeT's bugs (InvisiSpec spec-load L1D eviction, MSHR interference, I-cache/TLB) are collateral shared-resource leaks OUTSIDE SimSpect's scope (wrong observation channel + needs cache-occupancy stimulus Alloy can't gen). /work/amulet/*_AE = AMuLeT artifact builds; eviction toggle apply_spec_eviction_patch (default True=patched) at MESI_Two_Level-L1cache.sm:514. My check_ld_invisispec.py tests the VP↔squash race (complementary, not an AMuLeT bug). InvisiSpec ld run staged at results/STT_6_invisispec/ but PAUSED/not launched. See claudelog 2026-06-03.

2026-06-03 03:30 — 1a:inspectSTT — CORRECTED br_x mechanism (Anna caught my error): check_br DOES enforce lc/fnc window (my "window-gap" was wrong tick: compared to squash tick not fnc_retire). br_x FPs are because (a) unresolved=[] always for br_x → branches_unresolved vacuous, (b) check_br has NO taint/secrecy gate. The 46 redirect POST-resolution (non-secret) → STT correctly doesn't defend → FP. Real signal = load-side pre-resolution access. Verdict unchanged (FPs, 0 real branch leak) but reasoning fixed. Review md rewritten. Recommend check_br taint-at-redirect gate. See claudelog 03:30.
2026-06-03 01:45 — 3a:runManagerNew — STATE: (1) Fence neg-control sweep RELAUNCHED on SPT_6_oneLP (was wrongly SPT_6_full) both modes, pid 1050875, results/experiments/paper_fragility/fence_d*. (2) XML 200k regen: STT_6 DONE ~199,806; Recon_6 ~154k; SPT_6_oneLP ~107k (java still running). (3) testsetgen --follow STARTED for STT_6 (pid 1060426) streaming llvm+asm into testsets_latest/STT_6/<mode>/; will start Recon_6+SPT_6_oneLP gen as their xml finishes. Fragility STT result stands (plot plots/fragility_curve.png).

2026-06-03 02:45 — deepdiveBugCompilation — DONE: both runs now in the bucket_hits.py convention. Recon_6 → 183 check_ld FP / 1 recon_slf (inst-014912) / 0 NEW. stt_slowcomm_full → 21 check_ld FP / 19,845 stt_vp_squash_race / 4,122 NEW (=hits swept after the race_full rerun, NOT anomalies; re-run diag to close). ADDITIVE is_known on existing diag_stt_vp_squash_race.py (CLI intact); br_x (@1a) untouched. See claudelog 02:45.

2026-06-03 — invisispec — Read-only: defined InvisiSpec load-xmit criterion for SimSpect (build /work/amulet/amulet-gem5-InvisiSpec_AE-v1.1). It's the EXPOSE/VALIDATE packet (lsq_unit_impl.hh:1280, sent only after readyToExpose()=visibility point), NOT the invisible spec read (ReadSpecReq) that check_ld currently keys on. See claudelog 2026-06-03 entry. Nothing changed.

2026-06-03 01:30 — 3a:runManagerNew — Fragility done (STT /work/stt: clean threshold, leaks at commitToIEWDelay>=2, 0/20/41/42 not_taken d1-4; plot plots/fragility_curve.png). Stall sweep done = flat 0 on recon-modded (fixed check_ld, no leaks to act on; need leak-bearing regime). Launched FENCE negative-control sweep (gem5-spt scheme2=SpectreSafeFence, SPT_6_full not_taken, commitToIEWDelay{1-4}, env-driven NEW se_exp.py on gem5-spt). smoke d4 OK no livelock. pid 563547 exp results/experiments/paper_fragility/fence_d*. XML enum still going.

2026-06-02 23:42 — 3a:runManagerNew — Launched STALL-PARAM validation sweep (PAPER_EXPERIMENTS.md): pid 3334074, results/experiments/paper_stalls/, recon-modded scheme2 STT_6, 150 ld/mode seeded (same sample each pt). 3 sub-sweeps: s_U∈{0,50,250,1000,5000}, s_R∈{0,8,64}, fnc∈{0,150,300}. Extended run_sample.py copy w/ --resolved-stall (uniform over correctly_not_taken pcs) + --fnc-stall. Metric=real_cache_hits vs FP (cache-reach). log stall_sweep.log. Runs alongside fragility (3214670) + xml enums.

2026-06-02 23:40 — deepdiveBugCompilation — DONE: read-only synthesis of ALL bugs (target+checker+alloy+pipeline) into bug/BUG_TAXONOMY.md (by-root-cause-LAYER companion to BUGS.md) + bug/graphs/ (per-testset×implementation hit-attribution charts colored by category; make_graphs.py reproducible from disk buckets/claudelog). No code/model/run touched. See claudelog 23:40.

2026-06-02 23:35 — 3a:runManagerNew — Launched FRAGILITY sweep (EXPERIMENTS.md commitToIEWDelay threshold) in bg: pid 3214670, results/experiments/paper_fragility/, STT build /work/stt scheme2 via se_exp.py (env-driven), BARE unresolved=0, 300 ld/mode, d∈{1,2,3,4} SEQUENTIAL. log frag_sweep.log. Also drafted PAPER_EXPERIMENTS.md = stall-param (s_R/s_U/fnc) validation methodology vs labeled anchors (NOT the window — that's Alloy/settled). XML 200k enum still running (2967394/95/96).

2026-06-02 23:12 — 3a:runManagerNew — Re-enumerating XML for the 3 latest models (STT_6, SPT_6_oneLP, Recon_6, copied to latestmodels/) into SANDBOX testsets_latest/<model>/xml via pipeline.py xml --force (xml-only override configs in latestmodels/run_config_*_latest.jsonc, models_dir=latestmodels, testset_dir=testsets_latest). pids 2967394/95/96 (relaunched at cap=200k), logs latestmodels/<model>_xml.log. Touches NOTHING in live testsets/ or running sweeps.

2026-06-02 23:35 — 1a:inspectSTT — Started slowcomm diag (stt_slowcomm_full): 400/400 sampled ld hits = KNOWN stt_vp_squash_race, 0 NEW. ALIGNED diag_stt_vp_squash_race to check_ld's data-obtained gate (reached_cache OR store_forward-complete; was packet-only via classify_trace → missed SLF) — prior lone not_real_access flipped to a real race hit. Documented canonical leak gate + "diagnostics must reuse it" in CLAUDE.md (Leak criteria §). br_x (198+33) not yet bucketed.
2026-06-02 23:10 — 1a:inspectSTT — Ran diag on /work/stt STT runs (sttbuild + interleave_sttbuild): 0 leaks every kind/mode. Only anomaly = 348 "error" rows in interleave taken/ld → incomplete sweep (grid-pt-2 batch raw never written, checker hung ~09:18), NOT a bug. See claudelog 23:10.
2026-06-02 23:05 — reconHitDiag — Created repo-root bug/ catalog (BUGS.md, by build) + bug/recon_SLF_stlf_bypass/ verified example (inst-014912). check_ld fix (statusBar) confirmed to match the data-obtained/store_forward criterion. See claudelog 23:05.
2026-06-02 22:45 — statusBar — DONE: folded secondary "data-obtained" gate into check_ld (cache-packet OR store-forward completion; squash never gates; spec_read excluded). parse_lsq now grabs packet/spec ticks from the LSQUnit trace already on disk — no gem5 re-run. Fixed diag_ld_cache_reach (post-squash real access IS a leak, not over-count). ⚠️ corrects reconHitDiag 20:25 below — the 1 SLF among the 122 completes w/o a packet → REAL leak under the completion bar; the 122 need reclassifying by complete-before-squash. Load hit counts will DROP to genuine data-obtained on next run. Writeup: claudelog 22:45.

2026-06-02 20:25 — reconHitDiag — DONE verifying recon ld hits: fixed diag_ld_cache_reach.py to be sweep-aware (was dropping unresolved_stall=5000); faithful rerun = 0/122 reach cache → ALL check_ld FALSE POSITIVES (incl. the 1 SLF + 3 non-realized OTB). No real recon leak. See claudelog 19:22 update.

Each Claude session: jot a one-line status here of what you're doing *right now* so
other concurrent sessions know. Keep it brief. Newest at top. Format:

`YYYY-MM-DD HH:MM — <what I'm doing>`

---

2026-06-03 09:30 — 2a:inspectSPT — SPT slowcomm_fence FULLY TRIAGED + LOGGED (claudelog 09:30). LOADS: all no_hold = model/concretization bugs (2 owner-approved .als fixes: inaddr-cleanup line353 + stores-in-leakage_function; +3 diags), ~3807 genuine VP-race remain. BRANCHES: 0 leaks (wrong-path retq/loop re-spec + over-approx). TODO(owner): REGEN SPT_6_oneLP from fixed model; propagate fixes to SPT_6_full/SPT_6/oneLP_noSTLF; check_br window fix; br_x bucketing.


2026-06-03 08:00 — 2a:inspectSPT — MODEL FIX applied+verified (owner-approved): SPT_6_oneLP.als protset propagation now de-protects inaddr consumers not just inreg (line 353). Removes the inst-021701 class (value also leaked non-speculatively). Differential Alloy: bug SAT→UNSAT, normal gen + race shape preserved. TODO(owner): same fix for SPT_6_full/SPT_6/oneLP_noSTLF; REGEN SPT_6_oneLP testset to take effect. See claudelog 08:00.


2026-06-03 04:30 — 7:amulet — Cloned sith-lab/amulet -> /work/amulet-repo; py3.11 venv /opt/amulet-venv. Wrote src/gen_stt_tests.py (generates STT campaign corpus, NO gem5) + src/analyze_gadgets.py (PURELY STATIC iced-x86 decode: taint chains + critical-path window schedule). 500-test corpus: window-checked LL=18%% BRX=4%% either=20.4%% (loose/structure-only 69/30/71%%). Robust to load-lat. Writeup /work/amulet-repo/STT_GADGET_ANALYSIS.md.


2026-06-03 00:30 — 2a:inspectSPT — SPT slowcomm_fence FULLY bucketed via official diag_stt_vp_squash_race.py (in diag folder): 4502 VP-squash-race + 64 stt_no_hold (ALL ground-truth-verified harmless) + 5 check_ld-FP = 0 unexplained NEW. Race is slow-comm-only (holds at stock). br_x separate. See claudelog 00:30.


2026-06-02 23:58 — figureoutwhynocoinc — DONE (read-only): coincidence sweep (Run B) gets 0 hits because every test has exactly 1 unresolved branch (scanned 5k anns → {1:5000}); resolved branches are correctly_not_taken (no redirect). Known STT bug needs 2 colliding redirects → needs ≥2 unresolved branches per test = .als change (owner gate). See claudelog 23:58.

2026-06-02 23:55 — 2a:inspectSPT — REVISED+DONE: SPT "Fence leaks pre-squash" = VP↔squash race (NOT memory untaint — shadowL1 OFF; every load taints dest, rob:240). Genuinely fenced transmitter released at branch resolution, packet beats squash by 1-2cyc. SLOW-COMM-ONLY: 0/230 tainted-addr gadgets race at stock se.py (squashed before execute; fence holds). Deep-verified inst-000005 stock-vs-slowcomm. See claudelog 23:30+23:55.

2026-06-02 22:45 — 2a:inspectSPT — DONE: diag pipeline+cache test on oneLP_fence + slowcomm_fence. BOTH 0 real leaks. Root-caused the slow-comm "Fence leaks pre-squash" open case → zero-init prologue (movq $0,N(rsp)) untaints all shadowL1 → no DDIFT taint source → testset realizability gap, NOT a Fence bug. See claudelog 22:40. (superseded note below)

2026-06-02 22:10 — 2a:inspectSPT — running diag pipeline (bucket+cache test) on SPT_6_oneLP_fence__20260602_013615 + experiments/spt_slowcomm_fence; will root-cause leftover NEW/unknown. Read-only on running watchers.


2026-06-02 18:50 — runManager: Run A SPT-Fence CONTROL cache-reach diag done → Fence shows REAL pre-squash speculative cache accesses (93-95% slow-comm, 60-53% STOCK se.py on same stems) — present at stock too, so NOT a slow-comm artifact. Root cause OPEN, handed to HIT-INSPECTING claude: check #1 const-addr/untainted-addr over-approximation (diag exists: results/SPT_6_oneLP_fence__20260602_013615/diagnostics/diag_constaddr_load.py), #2 rule out Fence-not-engaged (config.ini). Data: /tmp/A_cachereach/{mode}/{cache_reach.json,cache_reach_stock.json,_hits.json}. Run A control UNSETTLED until this resolves. See claudelog 18:50.

2026-06-02 12:34 — runManager: LAUNCHED 4 PRIORITY SWEEPS + queued recon behind them. (A) SPT Fence slow-comm = EXPERIMENTS.md Fence control: full SPT_6_oneLP corpus on gem5-spt under STT slow-comm regime (NEW /work/gem5-spt/configs/example/se_slowcomm.py, faithful port of STT one: iewToCommitDelay=30/commitToIEWDelay=4/back+fwdComSize=40/squashWidth=1; smoke-tested NO livelock at 4 on gem5-spt). cfg results/experiments/spt_slowcomm_fence/, pid 3139363. (D) FULL-corpus STT slow-comm (was only ever a 600-sample) STT_6 /work/stt, results/experiments/stt_slowcomm_full/, pid 3139364 — already reproducing ~ sample hit rate. (B) STT resolution-COINCIDENCE sweep STT_6 + STT_6_interleave /work/stt se.py: dense short s_R points [0,1,2,4,8,16,32,64,128,256] × SMALL s_U via NEW sweep keys, random-sampled. results/experiments/stt_coincidence/{STT_6,STT_6_interleave}/, pids 3370246/3370247. (C) Recon (=scheme2 STT + --allow_leaked) DEPRIORITIZED per Anna: regen killed at 30,391/100k asm (resumable), QUEUED behind via nohup queue_recon_behind.sh (pid 3563103, fires when 1-min load<35 x2, nice 19). ⚠️ PIPELINE CHANGE (gated, backward-compat): pipeline.py _grid_points/_inject_stalls now accept sweep.unresolved_points (list→sweep s_U folded under "__unresolved__") + sweep.max_grid (random seeded sub-sample of the Cartesian blow-up). Absent keys = identical legacy behavior; existing sweeps unaffected (unit-tested). STT_6_interleave taken asm FINISHED 400k. See claudelog 12:34.

2026-06-02 11:30 — runManager: TESTSET-COMPLETENESS AUDIT (all gens dead — only live java is a6CountOnly STT_6.als, a read-only OTB counter, NOT a builder). Findings: STT_6 (non-il) COMPLETE 93,901/100k both modes (gap=known dup-PC). SPT_6_oneLP COMPLETE 96,943/95,991 (gap=dup-PC). STT_6_interleave not_taken COMPLETE 400k; **taken DIED mid-asm at 385,720/400k (14,280 missing tail from ~inst-081574)** → RESTARTED asm (resumable, taken-only cfg /tmp/asm_taken_il.jsonc, pid 2879833, log pipeline_STT_6_interleave_taken_asm.log; ok climbing, 0 err). **SPT_6_full BADLY INCOMPLETE: not_taken asm 28k/100k, taken asm 0/100k (asm died early, taken never ran)** — left alone per Anna (not restarted). So SPT_6_full is NOT usable for any sweep yet.

2026-06-02 — analysis-claude (inspectHitsSPT): PENDING / COME BACK TO — verify the remaining pure-LL SPT undiagnosed hits in gem5 (9 of 13: inst-008554/008558/008268/008270/008273/008537/008542/008546/008557). inst-008253 (STLF) confirmed check_ld false-positive (no cache access). Pure-LL ones: address from an uncommitted-but-pre-mispredict (untainted) load — strong structural evidence they are NOT leaks, but each not yet run individually. Also pending: diagnostic refinement (uncommitted->under-mispredict) + decision on bucketing check_ld STLF/fence over-reports via diagnostics rather than fixing check_ld.

2026-06-02 04:30 — designconsultant: Building NEW "slow-comm" experiment (NEW watchers, NEW se variant — existing watchers/se.py UNTOUCHED). Added /work/stt/configs/example/{se_exp.py (env-driven), se_slowcomm.py (hardcoded)} + results/experiments/slow_comm_squash1/{STT_6,STT_6_interleave}/run_config.jsonc. FINDING: commitToIEWDelay>=5 LIVELOCKS this gem5 (generic, not STT/not hook) — capped at 4; ROOT-CAUSED: STT visibility-point↔squash race (taint clears at branch resolution, wrong-path load accesses cache before squash drains). Driver = commitToIEWDelay≥2-3 (NOT squashWidth); stock gem5 does NOT leak; fnc-hook & check_ld-FP & model-over-approx all RULED OUT. Bug+diag shipped: results/experiments/slow_comm_squash1/diagnostics/{stt_vp_squash_race.md, diag_stt_vp_squash_race.py} (186/187 not_taken attributed). Watchers stopped; experiment via run_sample.py. Existing watchers/se.py untouched throughout. See claudelog 09:30.

2026-06-02 — reconbugs: Diagnosed recon-modded STT_6 ld hits (both modes, interleave + non). NOT SLF/OTB — 0 hits have a store; SLF-shaped tests simply not swept yet (only ~10k/100k done, 1 SLF-shaped in that batch). 100% of current ld hits are load→load = **check_ld FALSE POSITIVES**: STT bounces the tainted xmit load into delay_unit (lsq_unit.cc:2104) before sendPacketToCache (:2140); check_ld keys on the earlier "Executing load" tick (:725). Read-only analysis; touched NOTHING in the running watchers/checkers. Diagnostic folders created in both results dirs (XML SLF/OTB classifier only; no blind buckets). See claudelog. AWAITING user decision: fix check_ld (like check_br) vs. diagnostic bucket. FOLLOW-UP: OTB/getOldestTaint gadget is NEVER generated (0/100k base XML have ld→other→xm rf; address always direct-from-load) — model-minimality blocks the free combiner operand interleave needs. Wrote mdsToRead/OTB_GENERATION_GAP.md (root cause + dataflow-analysis pitfalls), linked from CLAUDE.md. Read-only; no model/pipeline edits. ⚠️ MAJOR FOLLOW-UP: the STT_6 + STT_6_interleave TESTSETS ARE STALE (gen 05-19 02:44, pre-effective-RS): 40% isolated no-op ops, 8.6x redundancy, only **2 leak dataflows**. Current model (RS active) gives 0% isolated / 1.4x / **45 dataflows** (proven: toggling the `all s:State|secure[RS->s]` line flips 0%↔38% isolated). The bloat I chased + "WIN1" are REDUNDANT — user's RS already fixes it. Recommend REGEN STT testsets from current model (from-XML relaunch) for ~22x more leak-topology coverage. OTB conclusion unchanged (other→xmit 0/2000 fresh too). NOT touched running sweeps. See claudelog.

2026-06-02 05:12 — runmanagernew: System was at load ~137 (64 cores) — NOT frozen, just CPU-oversubscribed (7 gem5 watchers: my 5 + designconsultant's 2, + 3 asm gens = 36 gem5). Paused MY 2 recon-modded STT watchers (4165134/4165135) → gem5 36→16, runnable threads 30<64 (no longer saturated). Resumable via `testrun.py ... --resume` (campaigns results/STT_6__latest, STT_6_interleave__latest intact). Left other session's watchers + asm gens alone. Heads-up: avoid stacking >~4 gem5 sweeps + asm gens at once on this box.
2026-06-02 02:08 — runmanagernew: STT_6_interleave mispredict_taken asm was at 0 (legacy asm gen does modes sequentially — still mid not_taken). Kicked off a parallel taken-only asm gen (pid 173028, temp cfg /tmp/asm_taken_il.jsonc, branch_modes=[mispredict_taken]) — now climbing. taken llvm was already done (400k). STT_6 taken already complete. Two interleave asm gens now (3473033 not_taken, 173028 taken) — no conflict.
2026-06-02 01:58 — runmanagernew: Added recon-modded gem5 watchers for STT (testrun pids 4165134 STT_6, 4165135 STT_6_interleave → /work/gem5-recon-modded, results/STT_6__<ts> & STT_6_interleave__<ts>). STT testsets now sweep BOTH /work/stt (_sttbuild) and recon-modded in parallel, type-sorted. 5 gem5 watchers total + 1 interleave asm gen. Deleted the duplicate SPT self-sweep results/SPT_6_oneLP/. See claudelog 01:58.
2026-06-02 01:45 — runmanagernew: Killed the leftover SPT `pipeline.py all` monolith (3474228) — it had finished xml+llvm and was redundantly self-sweeping Fence gem5 (the "SPT finding hits in build"). Also killed the SPT --follow follower (1801844): SPT asm done (96,943/95,991 of 100k). SPT now = fence testrun watcher only. Reminder: `pipeline.py all` couples gen+gem5; use testsetgen+testrun for the split. See claudelog 01:45.
2026-06-02 01:36 — runmanagernew: Restarted all 3 gem5 watchers (STT_6 3673944, STT_6_interleave 3673945, SPT fence 3673946) via testrun.py — new branch-fix checkers, results type-sorted into results/<name>__<ts>/<mode>/<kind>/. Old pre-fix results archived to results/prebranchfixResults/. invispec NOT restarted (fence-only). Generators untouched. STT_6 (non-interleave) gen DONE: 93,901/100k asm per mode (6,099 dropped by dup-PC bug, recoverable later). See claudelog 01:36.
2026-06-01 23:40 — opus-claude: PC-numbering audit DONE (read-only). ~96k tests / 11 testsets / both modes / interleave variants / all xmit kinds: PC numbering CLEAN — 0 dup labels, 0 gaps, xmit/branch/commit PCs all resolve to real labels, offsets monotonic. Only anomaly = STL_bug/inst-000637-no-store (hand-crafted debug variant, pc=3 deliberately removed — not generator output). Note: two ann schemas coexist (old x86_*_offset vs new addr/func_base_addr) — both fully resolved, not a PC issue. See claudelog 23:40.
2026-06-01 23:18 — runmanagernew: Reloaded CLAUDE.md (now following claudelog discipline; logged my changes there). HEADS-UP: my in-flight gens (SPT_6_oneLP follower 1801844 + STT_6/interleave test-gen 3473032/3473033) all started BEFORE opus-claude's 22:50 parsexml fix → contaminated testsets. Holding for user sign-off on the llvm-phase relaunch. SPT invispec watcher stopped (load relief); fence-only now. See claudelog 23:18 + 23:00 + 20:17.
2026-06-01 22:10 — analysis-claude: VERIFIED a real Stage-2 codegen bug (duplicate-PC label) dropping ~6% of STT_6 br_x tests in the live regen. Full writeup + suggested fix at bottom of this file ("VERIFIED BUG" section) — handing to another agent. AUDIT #14's "dormant" claim is false. Now analyzing root cause of the real BR_tainted branch leaks (not touching the pipeline).
2026-06-01 22:00 — runmanagernew: Built + smoke-tested the producer/consumer split: `testsetgen.py` (pipelined xml+llvm+asm, no gem5) and `testrun.py` (typed watcher, results split by xmit kind into results/<name>__<ts>/<mode>/<kind>/). Makefile has gen/run/pipeline. Old `pipeline.py` + `_sweep_watcher.py` UNTOUCHED (current runs depend on them). Ad-hoc scripts → archive/. CURRENT runs still going under old framework: SPT_6_oneLP mid-XML, STT_6 + interleave in llvm/asm + 4 /work/stt+spt watchers. New framework is for the NEXT campaign.
2026-06-01 21:29 — runmanagernew: SPT_6_oneLP NOT stuck — still mid-XML. llvm/asm=0 expected: `pipeline.py all` finishes ALL xml before llvm starts. Don't kill SPT mid-XML (Alloy restarts from 0); llvm/asm/gem5 are per-stem resumable.

2026-06-01 22:50 — opus-claude: FIXED the duplicate-PC label bug in parsexml.py (the "VERIFIED BUG" below). Changed the xm-NOP queue at :737 to inject only when the xm branch is the LAST instruction (dropped the "or next is a branch" clause); updated the stale NOTE at the insert site. Verified inst-000001 (was dropped) now compiles to .s/.o, labels unique. The two dead twin clauses (:814/:834, unreachable behind the :749 continue) left as-is. ⚠️ RELAUNCH NEEDED: already-generated .ll keep old logic and dropped tests stay dropped — to recover the ~6% br_x, wipe/--force the llvm phase for affected models (SPT_6_oneLP, STT_6, STT_6_interleave) and regen llvm→asm onward. I did NOT touch any running process.

---

# VERIFIED BUG (for handoff): duplicate-PC label drops ~6% of STT_6 br_x tests — Stage 2 codegen

**Status:** real and firing in live `pipeline_STT_6.log` (mtime 2026-06-01). NOT dormant. This is
AUDIT.md #14, but AUDIT's "unreachable/dormant" claim is now **false**.

## Root cause — `STAGE2_compilation/parsexml.py`
- Branch-xmit rework added an **xm-override block at `:730-749`** handling *every* xmit branch
  (`if is_xm:` — **no `resolved` check**), `continue`s at `:749`.
- At **`:737-739`** it queues a fall-through NOP when the xmit branch is last **OR is immediately
  followed by another branch** (`instructions[idx+1].get("kind") in _BRANCH_KINDS`).
- NOP inserted at **`:850-865`** with `nop_pc = br_pc+1`, **PCs never renumbered**.
- If the xmit branch is followed by another branch, that branch already owns `br_pc+1`, so the NOP
  and the real instruction **both emit `__litmus_inst_XXX_pc<br_pc+1>`** →
  `error: symbol '...' is already defined` → assembler aborts → **test dropped**.

## Evidence
- Live `.ll`: `inst-017570`, `inst-014962` emit `_pc5:` twice (nop + real `xorq`); `inst-020383`
  at `_pc3:`.
- `pipeline_STT_6.log`: all 20 sampled compile errors are "symbol … already defined". Live counter
  at 38,000/100,000: **errors=2,289 (~6%)** and climbing.
- Couldn't census all 2,289 (live regen deletes `.ll`/`.s`), but path is deterministic; 20/20 match.

## Why AUDIT #14 / code comment `parsexml.py:876-887` is stale
They claim the NOP path "only runs for unresolved xm branches (resolved branches `continue`
above)" and STT_6 has none. False post-rework: the `:730` override intercepts ALL xm branches and
`continue`s at `:749` **before** the resolved-branch `continue` at `:789`.

## Impact / relaunch scope
- Systematic: drops exactly the br_x gadgets where the xmit branch is followed by another branch
  (~20% of br_x coverage). Stage 1 Alloy XML (stable since May 19) is fine.
- Fix `parsexml.py`, **relaunch from parsexml → llvm → asm onward**. The in-flight STT_6 /
  STT_6_interleave regen is producing the corrupted testset and must be restarted after the fix.

## Suggested minimal fix (needs owner sign-off)
Inject the NOP only when the xmit branch is truly last (`idx == len(instructions)-1`), not when the
next instruction is already a branch (a following branch is already something to fetch). I.e. drop
the `or instructions[idx+1].get("kind") in _BRANCH_KINDS` clause at `:737-739` (and the dead twin
clauses at `:814-816` / `:834-836`). Verify the fall-through speculative target stays valid.

## NOT bugs (independently verified, didn't trust AUDIT)
- All recorded STT hits are real `BR_tainted` branch leaks (0 artifact / 0 const-addr hits).
- Loads correctly fenced: every `ld` xmit has a tainted address, 0% hit.
- `check_br.py` hit definition is sound (redirect + fall-through squashed + redirect in
  (lc_retire,fnc_retire) + other branches unresolved).
